# ⚡ C++ LIBRARIES FOR DATA CENTER SECURITY (25 Libraries)

# === HIGH-PERFORMANCE COMPUTING ===
find_package(Boost 1.80 REQUIRED COMPONENTS
    system thread chrono regex program_options json
    filesystem iostreams serialization
)

find_package(TBB REQUIRED)                    # Intel Threading Building Blocks
find_package(OpenMP REQUIRED)                 # Parallel programming

# === CRYPTOGRAPHY & SECURITY ===
find_package(OpenSSL 3.0 REQUIRED)           # Cryptographic functions
pkg_check_modules(LIBSODIUM REQUIRED libsodium)  # Modern crypto library
find_library(CRYPTO++ cryptopp)              # Crypto++ library

# === NETWORKING ===
pkg_check_modules(PCAP REQUIRED libpcap)     # Packet capture
find_library(DPDK dpdk)                      # Data Plane Development Kit
pkg_check_modules(NETFILTER libnetfilter_queue)  # Netfilter integration
find_library(LIBUV uv)                       # Async I/O library

# === DATABASE & STORAGE ===
find_package(PostgreSQL REQUIRED)            # PostgreSQL client
pkg_check_modules(HIREDIS REQUIRED hiredis)  # Redis client
find_library(ROCKSDB rocksdb)               # Embedded database
find_library(LEVELDB leveldb)               # Key-value storage

# === MONITORING & OBSERVABILITY ===
pkg_check_modules(PROMETHEUS prometheus-cpp) # Prometheus metrics
find_library(JAEGER jaeger)                 # Distributed tracing
pkg_check_modules(SPDLOG spdlog)            # Fast logging library

# === SERIALIZATION & COMMUNICATION ===
find_package(Protobuf REQUIRED)             # Protocol buffers
pkg_check_modules(GRPC grpc++)              # gRPC framework
find_library(MSGPACK msgpack)               # MessagePack serialization
pkg_check_modules(ZEROMQ libzmq)            # Messaging library

# === SYSTEM INTEGRATION ===
find_library(SYSTEMD systemd)               # systemd integration
pkg_check_modules(DBUS dbus-1)              # D-Bus messaging
find_library(LIBVIRT virt)                  # Virtualization API

# === PERFORMANCE OPTIMIZATION ===
find_library(JEMALLOC jemalloc)             # Memory allocator
find_library(TCMALLOC tcmalloc)             # Google's memory allocator