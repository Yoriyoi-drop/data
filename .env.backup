JWT_SECRET_KEY=infinite-ai-security-jwt-secret-2024
API_SECRET_KEY=infinite-ai-api-secret-2024
DEBUG=true
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000

# Database Configuration
# NOTE: Update these PostgreSQL credentials to match your local PostgreSQL installation
# To find or reset your PostgreSQL password:
# 1. If you know the password, replace it directly below
# 2. If you don't know the password, you can reset it using pgAdmin or psql:
#    - Open pgAdmin or run psql from your PostgreSQL installation directory
#    - Execute: ALTER USER postgres PASSWORD 'newpassword';
# 3. Common default passwords for Windows PostgreSQL: blank (empty), 'postgres', 'root', 'admin'
# 4. Check your PostgreSQL installation documentation for the password you set during installation
DB_BACKEND=postgres
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=postgres  # Default PostgreSQL password - CHANGE THIS to your actual password
PG_DATABASE=infinite_ai
